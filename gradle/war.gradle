apply plugin: "war"

bootWar {
    mainClassName = "com.emu.bpm.proxy.BpmProxyApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
